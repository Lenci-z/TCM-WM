# .hermes.md — 中西医结合心血管康复系统 项目规则

## 项目定位
面向心血管疾病患者的、以"中医证型 × 心血管危险分层"双轴模型驱动的结构化康复方案与数字化全程管理系统。第一版临床实施以冠心病血运重建术后（CAD_PCI）人群为切入。

## 需求依据（唯一来源）
- `任务与设计/中西医结合心血管康复系统设计方案_v0.2.md`：技术设计框架稿，所有表结构、规则、阈值以此为准
- `任务与设计/TASK.md`：任务列表，唯一执行依据，按编号顺序逐个验收，0 FAIL 硬门槛

## 技术栈约定
- Python 3.11 + tkinter + SQLite，本地桌面工具（不引入 Web 框架）
- GUI 界面一律中文；预设/规则可视化可调，不藏在代码里
- 规则层（分层阈值/处方模板/预警规则/证型特征）全部配置化存储，不硬编码

## 架构硬约束（v0.2 第 5.1 节）
- 数据模型与规则表按多病种设计：`disease_category` 字段 + `disease_config` 参数集表
- 第一版仅启用 `CAD_PCI`，其余病种建条目不启用
- 医师审核签发为不可跳过环节（临床决策支持工具定位）

## 目录约定
| 目录 | 内容 |
|---|---|
| `app/` | 主程序（db.py、engine/ 规则引擎、ui/ 界面） |
| `data/` | SQLite 数据库（运行时生成，不入库）+ seed 种子数据 JSON（入库） |
| `docs/` | 说明文档（数据模型、规则库说明） |

## 文件与 Git 约定
- 产物一律放本项目目录，不用 D:\AI-Hermes\outputs\
- 操作前备份原始文件；git commit 按功能点拆分，中文提交信息
- `*.db` 运行时生成不入库；seed JSON 必须入库

## 验收标准
- 每项任务完成需自测通过并在 `任务与设计/TASK.md` 勾选
- 回归测试 0 FAIL 硬门槛

## 后续开发注意事项（源自架构问题分析报告 2026-08-03，详见 docs/）

**架构红线（不得违背）**
- 规则外置：阈值/模板/禁忌一律存库，不硬编码
- 数据库 schema 变更必须走 `db.py MIGRATIONS`（schema_version 机制），不得直接改 CREATE 或删库重建
- 医师签发不可跳过；缺数据保守判高危；多病种架构按设计文档保留（7 病种条目，仅启用 CAD_PCI，不删空壳）

**分层纪律**
- 新引擎函数**不接 `conn`**：接收参数集 dict、返回 dict（纯领域逻辑）；数据访问经 Repository 层
- GUI 层保持薄：回调只做编排，不做领域决策（病种、阈值等一律从数据源读取，不硬编码）

**安全与数据（正式部署前必须升级，当前不扩展）**
- 姓名/联系方式为 MVP 级加密（XOR，密钥在 config.ini）——**不新增敏感字段**；正式部署前升级 AES + 认证 + RBAC + 审计
- 单机无备份策略：定期手动备份 `data/rehab.db`；不设计依赖多用户/远程的功能

**测试纪律**
- 每次代码改动跑全量测试（`python -m unittest discover -s test`）0 FAIL
- tkinter 无自动化测试方案（无 DOM/accessibility）：领域逻辑务必放引擎层保持可单测；GUI 回调抽纯函数

**外部集成（按需推进，需接口文档）**
- HIS/LIS/可穿戴/消息推送等临床生态集成需医院/厂商接口文档，不臆造接口、不写死模拟数据

**已知技术债（扩大使用前评估）**
- tkinter 局限：无图表组件、无安装包方案、无多显示器支持；Web 化/PyQt 迁移决策保留
- 单机 SQLite：单写者锁、数据孤岛；多用户需数据层整体重写（届时引擎已解耦则只换 Repository）
