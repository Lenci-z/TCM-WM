# 2026-08-03 开发记录

## 概述

项目首日开发：以 `中西医结合心血管康复系统设计方案_v0.2.md` 为唯一需求来源，完成**初级框架（可运行 MVP 原型）**全量构建——数据库、规则引擎、GUI 三端就位，并完成代码验收与修复闭环。

**技术路线**：Python 3.11 + tkinter + SQLite 本地桌面工具（与既往项目同栈）。
**硬约束**（v0.2 第 5.1 节）：架构宽、数据窄——全表 `disease_category` 字段 + `disease_config` 参数集，第一版仅启用 `CAD_PCI`。

## 时间线

### 上午：任务规划与阶段 0

1. 通读设计方案 v0.2（21.5K 字）与 IDEA.md，提炼关键约束（双轴模型/MVP 范围/规则外置/医师签发不可跳过）
2. 建立任务列表 `TASK.md`（阶段0-4 共 22 项，含验收标准），作为唯一执行依据
3. git 初始化（分支 main），首次提交 4 文件；建立目录结构 + `.hermes.md` 项目规则

### 阶段 1：数据模型与规则库（7 个任务）

| 内容 | 要点 |
|---|---|
| `app/db.py` | 17 张表（12 业务表 + 5 规则层表），全字段按文档 3.3；姓名/联系方式 XOR+base64 可逆加密 |
| 架构修正 | 分层阈值归入 `disease_config` 参数集 JSON（文档 3.3 定义），删除冗余的 `rule_stratification` 表，避免双源不一致 |
| 种子数据 | 7 病种（仅 CAD_PCI 启用）、危险分层阈值（高7/中4/低5）、6 证型特征、双轴矩阵 18 格模板（A1-F3，FITT-VP 全要素）、八段锦 L0-L3+、预警 16 条（红7/黄7/蓝2）、禁忌 10 条 |

**过程中发现并修复**：`import_seed` 遇到 dict 类型字段绑定失败（JSON 字段自动序列化修复）。

### 阶段 2：规则引擎（★核心IP★，6 个模块）

- `stratification.py`：高危→中危→低危逐级判定，满足任一即达本级；缺数据保守判高危（医疗安全取向）；6MWD 推算 METs（文献系数，标注需本院校准）
- `pattern.py`：四诊勾选关键词命中计数 → 主证 + 兼证
- `prescription.py`：矩阵编码（CAD_PCI-A2）→ 模板调取 → 阶段适配（I 期低强度/III 期维持）→ HRR 心率区间（k 法，β 阻滞剂用静息+20~30）→ 进阶/降级判定
- `safety.py`：通用/证型禁忌 + 病种特异禁忌，命中自动禁用抗阻、标注监护
- `alerts.py`：16 规则评估 + 适用范围过滤 + 触发→通知→处置→关闭全程留痕
- `pdf_export.py`：reportlab 处方单 PDF（五大处方整合输出样式 + 中文字体）

**过程中发现并修复**：模板表（A2）与处方表（CAD_PCI-A2）矩阵码前缀不一致。

### 阶段 3：GUI（tkinter，6 个视图）

- 主窗口（5 Tab 导航 + 状态栏 + 数据库自动初始化）
- 患者建档（patient+procedure 全字段，加密存储，列表/编辑/删除）
- 评估录入（全字段 + 四诊问卷自动证型判定 + 自动危险分层 + 舌象图片 + 历史回显）
- 处方管理（一键生成 → 医师调整 → 保存草稿 → 签发【签名必填不可跳过】→ 打印 PDF【未签发拦截】）
- 随访管理（手术日起算自动生成 1周/1月/3月/6月/12月 计划 + 到期提醒红黄标 + 复评录入）
- 规则库维护（7 类规则可视化编辑，JSON 校验，保存即时生效——引擎读库）

**过程中发现并修复**：ttk.Label 不支持 `fg`（两处）、grid/pack 混用布局冲突、历史评估列表 LEFT JOIN 一对多重复行。

### 阶段 4：测试与收尾

- `test/test_engine.py`：引擎回归 25 用例（分层5/证型5/矩阵3/处方6/安全3/预警5）
- `test/test_e2e.py`：虚构患者全链路（建档→评估→处方签发→PDF 103KB→随访→预警留痕）
- 主库清理至干净初始状态；`docs/数据模型与规则库说明.md` 落盘
- 全量验证：引擎 25/25 + e2e 1/1 + GUI 冒烟 全过

### 下午：代码验收（三权分立：用户 → Hermes → WorkBuddy）

WorkBuddy（Ada）生成 `docs/代码验收报告.md`，发现 4 个 P1 + 7 个 P2。

**核实结论**：全部属实（P1-2 经核实为"依赖导入顺序的脆弱耦合，运行时暂不崩"，仍按报告修复为显式导入）。

修复内容：

| 编号 | 修复 |
|---|---|
| P1-1 | `_load_form` 对 Entry 调 `.set()` 崩溃 → isinstance(Combobox) 分支 |
| P1-2 | `prescription_view` 显式导入 `simpledialog` |
| P1-3 | 保存处方改为全字段更新（医师调整全部持久化） |
| P1-4 | 预警引擎实现 `consecutive_met` 操作符（蓝色激励可触发） |
| P2-1 | 分层判定补 `heart_failure_symptom` 键名 |
| P2-2/3 | assessment/prescription 视图病种动态读取（`_disease_category()`） |
| P2-4 | `import_seed` 用 PRAGMA 识别真主键（替换字面量 "id" 死过滤） |
| P2-5 | `PHASE_RPE` 接入 phase_adjust（消除死代码） |
| P2-6 | 新增 `requirements.txt`（reportlab>=4.0） |
| P2-7 | 新增 `test/__init__.py` |

新增 `test/test_gui.py`（GUI 逻辑层回归 7 用例，覆盖全部 P1 修复路径）；验收报告追加第七章修复记录。

**复测**：`unittest discover` 33 用例 0 FAIL（引擎25 + e2e 1 + GUI 7）；主库未被测试污染。

### 傍晚：目录整理

按用户要求建立两个文件夹：

| 文件夹 | 内容 |
|---|---|
| `任务与设计/` | TASK.md + 设计方案 v0.2 + IDEA.md（git mv 归位，全链路引用已更新） |
| `开发记录/` | 本文档 + README 索引 |

## git 提交记录（23 次）

```
5aabb52 gitignore：.workbuddy 审查工具运行时记忆不入库
dd6dcf8 响应代码验收报告：修复 P1×4 + P2×7 + 新增GUI回归测试7用例（全量33测试0 FAIL）
cc52f00 修正：表数口径 19→17（12业务表+5规则层表），docs与TASK同步
dbba81b 收尾：主库清理至干净初始状态 + docs数据模型与规则库说明 + 最终回归验证
3fab320 阶段4：自动化测试（引擎回归25用例+全流程联调e2e）0 FAIL
898eabb 阶段3.6：规则库维护界面（7类规则可视化编辑，保存即时生效）
36286a4 阶段3.5：随访管理（计划自动生成/到期提醒/复评录入）
84869e7 阶段3.4：处方管理（生成/调整/签发/打印PDF）
68e7ea9 阶段3.3：评估录入（四诊问卷自动证型判定+自动分层）
008f789 阶段3.2：患者建档界面（加密存储/编辑/删除）
931abf6 阶段3.1：主窗口5Tab导航+数据库自动初始化
c6bd53c 阶段2.6：预警引擎（闭环留痕）+ 阶段2全部完成
64ea137 阶段2.5：安全校验层（禁忌自动禁用抗阻）
0b4408c 阶段2.3-2.4：矩阵编码+处方生成引擎
26cd308 阶段2.1-2.2：危险分层+证型判定引擎
f6ac4d2 阶段1.6-1.7：八段锦分级+预警规则16条
f89b257 阶段1.4-1.5：6证型特征+18格处方模板
a5d0d72 阶段1.3：CAD_PCI危险分层阈值参数集
cd00338 阶段1.2：disease_config 7病种条目+架构修正
2c0bd24 阶段1.1：db.py 建库17张表
8ee919c 阶段0.1：目录结构+.hermes.md
af5bcb1 初始化：设计方案v0.2、IDEA、TASK.md、.gitignore
```

## 当前状态

- **功能**：MVP 三件事全就位（建档+评估录入 / 处方生成+签发+打印 / 随访提醒+复评），规则库 GUI 可视化维护
- **测试**：33/33 用例 0 FAIL
- **主库**：干净初始状态（17 表 + 规则库完整，0 患者）
- **已知遗留**：数值范围校验（LVEF 0-100 等）、加密升级（对接医院方案）、六 MWDT 公式本院校准、GUI 端到端自动化（当前为逻辑层测试）

## 备注

- `.workbuddy/`（审查工具运行时记忆）已加入 .gitignore 不入库
- 测试全程使用独立测试库 `data/test_rehab.db`，主库零污染
