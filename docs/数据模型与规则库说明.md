# 数据模型与规则库说明

> 项目：中西医结合心血管康复方案包与数字化全程管理系统
> 依据：`任务与设计/中西医结合心血管康复系统设计方案_v0.2.md`（框架稿）
> 技术栈：当前 MVP 为 Python 3.11 + tkinter + SQLite（桌面版）；**P4 已选定路线 B（FastAPI + React Web）**，tkinter 出局三因（SQLite 单写者锁 / 桌面装不到患者端 / 无法自动化测试），待决策备忘签字后迁移

## 1. 目录结构

```
├── app/
│   ├── db.py                    # 数据库层：22 表 schema（12业务+5规则+安全3+迁移/系统2）+ 加密 + 种子导入 + schema_version 迁移
│   ├── repo.py                  # Repository 层：集中全部数据访问（P2 起引擎不执行 SQL）
│   ├── engine/                  # ★核心IP★ 规则引擎（纯逻辑：接收 dict、返回 dict，不碰 conn）
│   │   ├── stratification.py    #   危险分层判定（2.1）
│   │   ├── pattern.py           #   证型判定（2.2）
│   │   ├── prescription.py      #   矩阵编码 + 处方生成 + 进阶/降级（2.3/2.4）
│   │   ├── safety.py            #   安全校验层（2.5）
│   │   ├── alerts.py            #   预警引擎（2.6）
│   │   └── pdf_export.py        #   处方单 PDF 导出（reportlab + 中文字体）
│   ├── security.py              # AES-256-CBC 加密 + bcrypt 密码哈希（P3）
│   ├── auth.py                  # 登录 / 失败锁定 / RBAC 权限矩阵 / 审计日志（P3）
│   ├── backup.py                # 备份恢复（SQLite backup API）
│   ├── csv_export.py            # 患者/随访/审计日志 CSV 导出（UTF-8 BOM）
│   ├── config.py                # 配置读取（config.ini）
│   ├── log.py                   # 统一日志（落 data/logs/）
│   └── ui/                      # tkinter 界面（登录 + 7 Tab）
│       ├── main.py              #   主窗口 + 数据库自动初始化（入口）
│       ├── login_view.py        #   登录 / 首次初始化管理员（P3）
│       ├── patient_view.py      #   患者建档（3.2）
│       ├── assessment_view.py   #   评估录入（3.3）
│       ├── prescription_view.py #   处方管理（3.4）
│       ├── followup_view.py     #   随访管理（3.5）
│       ├── dashboard_view.py    #   数据看板（阶段7：统计概览 + 预警处置）
│       ├── adherence_view.py    #   依从性打卡（阶段7）
│       └── rules_view.py        #   规则库维护（3.6，仅管理员可见）
├── data/
│   ├── rehab.db                 # 主数据库（运行时生成，git 忽略）
│   ├── test_rehab.db            # 测试库（测试运行生成，git 忽略）
│   ├── logs/                    # 运行日志（不入库）
│   ├── backups/                 # 数据库备份（不入库）
│   └── seed/                    # 种子数据 JSON（入库管理）
│       ├── disease_config.json        # 7 病种参数集（仅 CAD_PCI 启用）
│       ├── rule_tcm_pattern.json      # 6 证型特征
│       ├── rule_rx_template.json      # 双轴矩阵 18 格处方模板（A1–F3）
│       ├── rule_baduanjin.json        # 八段锦 L0–L3+ 分级
│       ├── rule_alert.json            # 预警规则 16 条（红7/黄7/蓝2）
│       └── rule_contraindication.json # 禁忌规则 10 条
├── scripts/
│   └── migrate_encrypt.py       # XOR→AES 存量数据迁移脚本（P3）
├── 任务与设计/                  # 任务列表 + 设计方案 + 项目定位（唯一执行依据）
│   ├── TASK.md                  # 任务列表（按编号顺序验收，0 FAIL 硬门槛）
│   ├── 中西医结合心血管康复系统设计方案_v0.2.md  # 需求唯一来源
│   └── IDEA.md                  # 项目一句话定位
├── 开发记录/                    # 本地开发日志（按日期）
│   └── 2026-08-03.md            # 首日开发记录（含 P2/P3/并行第一批）
├── test/                        # 141 用例（全量 0 FAIL）
│   ├── test_engine.py           #   引擎回归 25 用例
│   ├── test_engine_pure.py      #   引擎纯逻辑（P2，无 DB）
│   ├── test_alerts_operators.py #   预警 12 操作符覆盖
│   ├── test_repo.py             #   Repository 层（P2）
│   ├── test_security.py         #   安全 15 用例（P3）
│   ├── test_backup.py           #   备份恢复 5 用例
│   ├── test_db_health.py        #   DB 健康检查 6 用例
│   ├── test_stage7.py           #   阶段7 功能用例
│   ├── test_e2e.py              #   全流程联调（虚构患者）
│   └── test_gui.py              #   GUI 逻辑层回归
├── docs/                        # 设计文档 + 分阶段开发 PRD/任务列表 + 执行指令 + 验收报告
├── 启动系统.bat                 # Windows 一键启动（自动探测 Python + 依赖安装）
├── config.ini                   # 运行配置（DB 路径/密钥/医院名/字体/日志）
├── .hermes.md                   # 项目规则
└── requirements.txt             # reportlab / cryptography / bcrypt
```

## 2. 数据模型（22 表）

### 2.1 业务表（12 张，全部含 `disease_category` 字段——多病种架构）

| 表 | 说明 | 关键字段 |
|---|---|---|
| `disease_config` | 病种参数集（★v0.2 核心） | 分层阈值/运动禁忌/八段锦起始映射/随访模板/特有预警（JSON） |
| `patient` | 患者主表 | 姓名/联系方式加密存储（AES-256-CBC，正式版对接医院方案）；`medical_no` 病历号（CR-年份-4位序号，自动生成、不复用，对外显示用） |
| `procedure` | 手术/事件信息 | 类型/支架数/完全血运重建/切口类型/抗凝状态 |
| `assessment` | 评估记录 | 代谢全套（LDL-C/HbA1c/尿酸/eGFR/UACR）+ 心功能/运动/心理/行为 |
| `tcm_pattern` | 证型记录 | 主证/兼证/四诊JSON/舌象图/判定方式/医师确认 |
| `risk_stratification` | 危险分层 | 低/中/高 + 触发条件JSON + 参数集版本 + 医师确认 |
| `prescription` | 康复处方 | 矩阵编码/八段锦级别/有氧/抗阻/中医/营养/危险因素目标/签名/状态 |
| `adherence_log` | 打卡记录 | 任务类型/完成/时长/RPE/症状 |
| `vital_upload` | 自测数据 | 血压/心率/体重/步数等 |
| `alert` | 预警记录 | 红/黄/蓝 + 处置闭环（待处置→已关闭全程留痕） |
| `follow_up` | 随访计划与记录 | 1周/1月/3月/6月/12月 + 复评记录JSON |
| `clinical_event` | 临床事件 | 死亡/再入院/再次血运重建/心梗/卒中/出血 |

### 2.2 规则层表（5 张，★核心IP★ 规则外置）

| 表 | 内容 |
|---|---|
| `rule_tcm_pattern` | 6 证型特征（症状/舌象/脉象/关键词，供判定引擎匹配） |
| `rule_rx_template` | 双轴矩阵 18 格处方模板（FITT-VP 全要素 JSON） |
| `rule_baduanjin` | 八段锦 L0–L3+ 分级（体位/招式/估算METs/适用） |
| `rule_alert` | 预警规则（条件/适用范围/处置动作 JSON） |
| `rule_contraindication` | 禁忌规则（病种/证型/分层 * 通配） |

> 注：危险分层阈值、病种特异禁忌、八段锦起始映射、随访节点模板存于 `disease_config` 参数集 JSON（文档 3.3 定义），规则引擎统一从参数集加载，避免双源不一致。

### 2.3 安全、迁移与系统表（5 张）

| 表 | 说明 |
|---|---|
| `user` | 用户账号（bcrypt 密码哈希，非明文） |
| `role` | 角色定义（医生/护士/管理员，RBAC 权限矩阵） |
| `audit_log` | 审计日志（登录/敏感操作留痕） |
| `schema_version` | schema 迁移版本记录（MIGRATIONS 列表，加字段不删库） |
| `sys_counter` | 系统计数器（MIGRATIONS v3：病历号序号持久化，删除患者不回退） |

## 3. 规则库内容（CAD_PCI 第一版）

| 规则 | 数量 | 说明 |
|---|---|---|
| 危险分层阈值 | 高7/中4/低5 | 满足任一即达本级，按 高危→中危→低危 判定；缺数据保守判高危 |
| 证型特征 | 6 证型 | 四诊勾选 → 关键词命中计数 → 主证+兼证 |
| 处方模板 | 18 格（A1–F3） | II 期基线；I/III 期引擎阶段适配；强度按分层（k 0.4–0.7） |
| 八段锦分级 | 5 级（L0–L3+） | 起始映射：高危L0/中危L1/低危L2；升降级条件 |
| 预警规则 | 16 条 | 红7（24h处置）/黄7（72h）/蓝2（仅患者端） |
| 禁忌规则 | 10 条 | 通用+证型级（肝阳上亢禁抗阻等）；CAD_PCI 病种特异 3 条（穿刺点/DAPT） |
| 随访模板 | 5 节点 | 手术日+7/30/90/180/365 天，必查项+选查项 |

## 4. 引擎执行流程（文档 3.4）

```
[数据输入]
  ↓
[病种识别] → 加载 disease_config 参数集
  ↓
[分层判定] ← 危险分层阈值（病种特异）
  ↓ ← 证型判定（四诊问卷 → 关键词匹配）
[矩阵编码 CAD_PCI-A2]
  ↓
[处方生成] ← 处方模板库 + 阶段适配（I/II/III）+ 心率区间（HRR k 法）
  ↓
[安全校验] ← 通用禁忌 + 病种特异禁忌（命中自动禁用抗阻等）
  ↓
[医师审核签发] ★人工确认必经环节，不可跳过★
  ↓
[打印 PDF]（reportlab 五大处方整合输出）
  ↓
[执行数据回流] → [预警引擎] → 进阶/降级判定 → 回到处方生成
```

> **P2 解耦说明**：上述流程不变，但数据访问已从引擎剥离——GUI 回调 → `repo` 取数 → 引擎纯函数（dict→dict）→ `repo` 存结果。引擎 6 模块不再接收 `conn` 参数、不执行 SQL，可脱离 DB 单测。

## 5. 运行与测试

```bash
# 启动系统（桌面版）
python app/ui/main.py

# 全量回归（141 用例，独立测试库 data/test_rehab.db，主库零污染）
python -m unittest discover -s test -v
```

**0 FAIL 硬门槛**：任何规则/代码修改后重跑全量测试确认无回归。

## 6. 关键技术要点

1. **多病种架构**（v0.2 硬约束）：所有表带 `disease_category`，第一版仅启用 CAD_PCI，其余 6 病种建条目不启用；扩展只需填规则表，不改代码
2. **规则外置**：全部规则存表/JSON，GUI 规则库维护界面可视化编辑，保存即时生效（引擎读库）
3. **引擎纯逻辑化**（P2）：引擎函数接收参数集 dict、返回 dict，不依赖数据库连接——为 P4 技术栈迁移（路线 B）预留最低迁移成本
4. **无 CPET 处方路径**：主观强度主导（RPE/谈话试验）+ 客观校验（HRR），服 β 阻滞剂时心率粗略上限 静息+20~30，以 RPE 为准
5. **6MWD 推算 METs**：peak VO₂ ≈ 4.948 + 0.023×6MWD；METs = VO₂/3.5（文献默认系数，**需本院数据校准——校准系数是技术秘密**）
6. **加密**（P3 升级）：姓名/联系方式 AES-256-CBC 加密（密钥环境变量 REHAB_ENCRYPT_KEY 或 data/.secret_key，不入库）；密码 bcrypt 哈希；存量 XOR 数据经 `scripts/migrate_encrypt.py` 迁移
7. **认证与权限**（P3）：登录 + 失败锁定（5次/15分钟）+ RBAC 权限矩阵（医生/护士/管理员）+ 审计日志
8. **合规设计**：医师审核签发不可跳过，系统定位"临床决策支持工具"，规避 SaMD 医疗器械注册问题

## 7. 待团队确认事项（文档第六章摘要）

- 本院 PCI 术后实际证型分布（决定矩阵主力格）
- 危险分层阈值采用 AACVPR 标准或本院修订
- 各证型对应院内制剂（冠心平、抗栓I号等）
- 预警阈值具体取值（科室共识 + 伦理认可）
- 八段锦各级别招式取舍与实测能耗（本院数据 = 核心技术秘密）
- 专利 FTO 检索、权属双备案（学校科技处 + 医院科教处）
- **P4 路线决策**：多用户访问 / 院外远程随访 / Web 端 3 决策项（`docs/P4路线决策备忘.md`，待老师确认后签字启动路线 B）

*文档版本 v0.2 ｜ 2026-08-03 ｜ 与设计方案 v0.2 对应 ｜ 同步 P2 解耦 / P3 安全 / 阶段7 临床功能*
